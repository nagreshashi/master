# This is my reusable worflow

on:
  workflow_call:
    inputs:
      env:
      required: true
      
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    
     - name: Checkout Repository
       uses: actions/checkout@v1

     - name: Deploy to ${github.event.inputs.env}
       run: |
         echo "Deploying to ${github.event.inputs.env}"
